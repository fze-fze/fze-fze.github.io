{
  "name": "newblog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "habit:sleep": "node scripts/update-habit-checkin.mjs --habit sleep",
    "habit:mindfulness": "node scripts/update-habit-checkin.mjs --habit mindfulness"
  },
  "dependencies": {
    "astro": "^5.7.4"
  }
}
